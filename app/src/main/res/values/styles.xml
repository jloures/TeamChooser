<resources>

    <style name="ActionBarBaseTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:titleTextStyle">@android:color/white</item>
        <item name="android:elevation">4dp</item>
        <item name="android:background">@color/blue800</item>
        <item name="android:homeAsUpIndicator">@drawable/back_button_white</item>
    </style>

</resources>
